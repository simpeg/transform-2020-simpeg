name: t20-tue-simpeg

channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - scipy
  - matplotlib
  - ipywidgets>=0.6.0
  - mkl
  - pip
  - pip:
    - git+https://github.com/simpeg/simpeg.git@transform-dc-updates
